diff a/src/components/StackSection.astro b/src/components/StackSection.astro	(rejected hunks)
@@ -5,6 +5,7 @@ interface Technology {
     category: string;
     color?: string | null;
     icon_slug?: string | null;
+    logo_url?: string | null;
 }
 interface Props {
     technologies: Technology[];
@@ -31,15 +32,35 @@ const groupedTech =
                     {category}
                 </p>
                 <div class="flex flex-wrap gap-3 md:gap-2">
-                    {techs.map((tech) => (
-                        <span
-                            class="inline-block truncate max-w-full font-mono text-xs px-4 py-2 md:px-3 md:py-1.5 transition-all duration-200 hover:text-(--text-primary) hover:border-(--text-secondary)"
-                            style="color: var(--text-secondary); border: 1px solid var(--border-default);"
-                            title={tech.name}
-                        >
-                            {tech.name}
-                        </span>
-                    ))}
+                    {techs.map((tech) => {
+                        const colorHex = (tech.color || '#888888').replace('#', '');
+                        const iconSrc = tech.logo_url
+                            ? tech.logo_url
+                            : tech.icon_slug
+                            ? `https://cdn.simpleicons.org/${tech.icon_slug}/${colorHex}`
+                            : null;
+
+                        return (
+                            <span
+                                class="inline-flex items-center gap-2 font-mono text-xs px-4 py-2 md:px-3 md:py-1.5 transition-all duration-200 hover:text-(--text-primary) hover:border-(--text-secondary)"
+                                style="color: var(--text-secondary); border: 1px solid var(--border-default);"
+                                title={tech.name}
+                            >
+                                {iconSrc && (
+                                    <img
+                                        src={iconSrc}
+                                        alt={tech.name}
+                                        width="14"
+                                        height="14"
+                                        loading="lazy"
+                                        style="object-fit: contain; flex-shrink: 0; opacity: 0.75;"
+                                        onerror="this.style.display='none'"
+                                    />
+                                )}
+                                {tech.name}
+                            </span>
+                        );
+                    })}
                 </div>
             </div>
         ))
